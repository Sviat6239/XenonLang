# Example of a private class
private class Secret {
    var code: int = 123;  # Private variable
    fun reveal(): int {
        return code;
    }
}

# Testing the class (access restricted to the module)
var s: Secret = new Secret();
print(s.reveal());  # Output: 123 (access only within the module)