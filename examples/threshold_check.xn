# Var Declaration with Loop
# Sums numbers from 1 to 10.

# Declares a mutable variable 'sum' initialized to 0.
var sum: int = 0;
# Loops from 1 to 10.
for (var i = 1; i <= 10; i = i + 1) {
    # Adds 'i' to 'sum'.
    sum = sum + i;
}
# Prints the final sum.
print("Sum: " + sum);
